import Vue from "vue";

export default Vue.extend({
    props: {
    },
    data: function(): any {
        return {
        };
    },
    methods: {
        searchClicked(): void {
            
        }
    }
});
